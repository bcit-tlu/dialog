{{ .Chart.Name }} ({{ include "dialog-backend.fullname" . }}) has been deployed.

Roles deployed from image {{ include "dialog-backend.image" . }}:
  - api     (Service {{ include "dialog-backend.fullname" . }}:{{ .Values.service.port }})
{{- if .Values.worker.enabled }}
  - worker  (no Service — consumes jobs from Redis)
{{- end }}
{{- if .Values.gateway.enabled }}
  - gateway (Service {{ include "dialog-backend.fullname" . }}-gateway:{{ .Values.gateway.port }})
{{- end }}

Dependencies:
  - PostgreSQL (CNPG): {{ if .Values.postgres.enabled }}in-cluster{{ else }}external ({{ .Values.postgres.uri }}){{ end }}
  - Redis:             {{ if .Values.redis.enabled }}in-cluster{{ else }}external ({{ .Values.redis.url }}){{ end }}
  - Object storage:    {{ if .Values.minio.enabled }}in-cluster MinIO{{ else }}external S3 ({{ .Values.minio.endpointUrl }}){{ end }}

Check the API health from within the cluster:

  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "dialog-backend.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}
  curl http://localhost:{{ .Values.service.port }}/health
